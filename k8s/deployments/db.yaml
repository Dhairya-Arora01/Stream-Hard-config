apiVersion: v1
kind: Pod
metadata:
  name: db-pod
  labels:
    component: db
spec:
  containers:
    - name: pg
      image: postgres:alpine
      env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysecret
              key: password
      ports:
        - containerPort: 5432